//
//  History.swift
//  CoffeeCozy
//
//  Created by Zdeněk Svoboda on 23.05.2025.
//
import Foundation
import UIKit
/*
struct OrderHistory: Identifiable {
    var id: UUID
    var date: Date
    var user: User
    var products: [Product]

    static let sample1 = OrderHistory(
        id: UUID(),
        date: Date(),
        user: User.samples[0],
        products: [Product.samples[0], Product.samples[1]]
    )

    static let sample2 = OrderHistory(
        id: UUID(),
        date: Calendar.current.date(byAdding: .day, value: -3, to: Date())!,
        user: User.samples[1],
        products: [Product.samples[1]]
    )

    static let samples = [sample1, sample2]
}
*/
